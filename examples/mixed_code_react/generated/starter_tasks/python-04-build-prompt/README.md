# Build a deterministic CSS prompt

build_prompt(case, html) must include the CSS filename, visual description, and immutable HTML. It must explicitly instruct the model to return CSS only, avoid external assets, use an 800x600 viewport, and avoid overflow.
