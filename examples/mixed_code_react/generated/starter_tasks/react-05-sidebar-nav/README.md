# Sidebar navigation

Create a 220px-wide, full-600px-height sidebar on the left of an #f8fafc page. Use Arial. The sidebar is #111827 with 24px padding using border-box sizing. The white 20px bold logo appears at the top. Navigation begins 36px below and has 10px gaps. Each link is a 44px-high flex row with 12px horizontal padding, 10px radius, and #cbd5e1 text. The active item has #2563eb background and white bold text. Keep the small user block pinned to the bottom; its avatar is a 36px #f59e0b circle.

Implement `src/styles.css`. You may adjust `src/App.jsx` only if needed for semantic React markup.
